            //将数据在表格中显示出来
            DataTable data = new DataTable();
            for (int column = 0; column < 50; column++)
                data.Columns.Add(column.ToString(), typeof(string)); //新增列；列名显示二维数组的第一个元素-标题

            List<Series> seriesList = new List<Series>();
            
            for (int row = 1; row < newdata.GetLength(0); row++) //填充行数据
            {
                string seriesName = "route" + row.ToString();
                Series series = new Series(seriesName);
                series.ChartArea = "C2";//添加到
                chart1.Series.Add(series);
                this.chart1.Series[row - 1].ChartType = SeriesChartType.Spline;//指定图标样式
                chart1.Series[row - 1].Points.Clear();    //清除所有点

                for (int column = 0; column < newdata.GetLength(1); column++)
                {
                    //Console.WriteLine("{0}  {1}", newdata[0, column], newdata[row, column]);
                    chart1.Series[row-1].Points.AddXY(newdata[0, column], newdata[row, column]);
                    
                }
                //seriesList.Add(series);

            }